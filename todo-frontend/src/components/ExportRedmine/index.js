import React from 'react';
import { Button } from '@material-ui/core';

const ExportRedmine = () => {

    return (
        <div className ="ExportToRedmineButton">
            <Button variant="contained" >Export to Redmine</Button>
        </div>
    );
}

export default ExportRedmine;